{
  "name": "baidu-traffic",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run clean && node index.js",
    "clean": "rm -rf ./imgs && rm -rf ./log && mkdir imgs && touch log",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/node-schedule": "^1.2.0",
    "node-schedule": "^1.2.3",
    "phantom": "^4.0.4"
  }
}
